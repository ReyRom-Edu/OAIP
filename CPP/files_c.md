# 📘 Лекция: Работа с файлами в языке C

Работа с файлами — одна из ключевых задач в языке C.
Файлы позволяют программе **сохранять данные между запусками**, **обмениваться информацией с другими программами**, а также **обрабатывать большие объёмы данных**, которые невозможно хранить только в оперативной памяти.

В языке C работа с файлами осуществляется через **стандартную библиотеку ввода-вывода** (`<stdio.h>`), которая предоставляет универсальный интерфейс для взаимодействия с файлами на уровне операционной системы.

Основой работы с файлами является **указатель на структуру `FILE`**, которая описывает открытый поток данных и хранит информацию о текущей позиции, режиме открытия и других параметрах.

## Основные функции для работы с файлами

| Функция     | Назначение                                   |
| ----------- | -------------------------------------------- |
| `fopen()`   | Открывает файл                               |
| `fclose()`  | Закрывает файл                               |
| `fread()`   | Читает бинарные данные                       |
| `fwrite()`  | Записывает бинарные данные                   |
| `fprintf()` | Записывает форматированные данные            |
| `fscanf()`  | Читает форматированные данные                |
| `fseek()`   | Перемещает указатель чтения/записи           |
| `ftell()`   | Возвращает текущую позицию в файле           |
| `feof()`    | Проверяет достижение конца файла             |
| `ferror()`  | Проверяет наличие ошибки при работе с файлом |

## Открытие файла — `fopen`

Функция `fopen()` используется для открытия файла.
Она возвращает указатель типа `FILE*`.
Если файл открыть не удалось (например, его не существует или нет прав доступа), функция возвращает `NULL`.

### Синтаксис:

```c
FILE *fopen(const char *filename, const char *mode);
```

* **`filename`** — имя файла (путь к файлу).
* **`mode`** — режим открытия файла.

| Режим  | Назначение                                                       |
| ------ | ---------------------------------------------------------------- |
| `"r"`  | Открыть для чтения (файл должен существовать)                    |
| `"w"`  | Открыть для записи (создаёт новый или очищает существующий файл) |
| `"a"`  | Открыть для добавления данных в конец файла                      |
| `"r+"` | Открыть для чтения и записи (без удаления данных)                |
| `"w+"` | Открыть для чтения и записи (очищает файл)                       |
| `"a+"` | Открыть для чтения и добавления данных                           |

> 🔹 Для работы с бинарными файлами добавляется символ **`b`**:
> `"rb"`, `"wb"`, `"ab"`, `"r+b"`, `"w+b"`, `"a+b"`.

### Пример:

```c
FILE *file = fopen("data.txt", "r");
if (file == NULL) {
    printf("Ошибка при открытии файла!\n");
    return 1;
}
```

## Закрытие файла — `fclose`

Каждый открытый файл должен быть закрыт после окончания работы, чтобы освободить системные ресурсы.

### Синтаксис:

```c
int fclose(FILE *stream);
```

* Возвращает **0** при успешном закрытии и **EOF (-1)** при ошибке.

### Пример:

```c
fclose(file);
```

> ⚠️ Если не закрыть файл, данные могут остаться в буфере и не попасть в сам файл.

## Запись данных в файл

### Запись бинарных данных — `fwrite`

Используется для записи **произвольных блоков памяти** в файл.

```c
size_t fwrite(const void *ptr, size_t size, size_t count, FILE *stream);
```

* **`ptr`** — адрес блока памяти.
* **`size`** — размер одного элемента.
* **`count`** — количество элементов.
* **`stream`** — файловый поток.

#### Пример:

```c
FILE *file = fopen("numbers.bin", "wb");
if (file != NULL) {
    int numbers[] = {10, 20, 30, 40, 50};
    fwrite(numbers, sizeof(int), 5, file);
    fclose(file);
}
```

---

### Форматированная запись — `fprintf`

Позволяет записывать текст в файл с форматированием, аналогично `printf`.

#### Пример:

```c
FILE *file = fopen("info.txt", "w");
if (file != NULL) {
    fprintf(file, "Name: %s\nAge: %d\n", "Alice", 25);
    fclose(file);
}
```

> 💡 Часто используется для логов, таблиц или текстовых отчётов.


## Чтение данных из файла

### Чтение бинарных данных — `fread`

```c
size_t fread(void *ptr, size_t size, size_t count, FILE *stream);
```

* Читает данные в память, на которую указывает `ptr`.

#### Пример:

```c
FILE *file = fopen("numbers.bin", "rb");
if (file != NULL) {
    int numbers[5];
    fread(numbers, sizeof(int), 5, file);
    for (int i = 0; i < 5; i++) {
        printf("%d ", numbers[i]);
    }
    fclose(file);
}
```

---

### Форматированное чтение — `fscanf`

Используется для считывания текстовых данных по шаблону.

```c
FILE *file = fopen("info.txt", "r");
if (file != NULL) {
    char name[50];
    int age;
    fscanf(file, "Name: %s Age: %d", name, &age);
    printf("Name: %s, Age: %d\n", name, age);
    fclose(file);
}
```

---

### Построчное чтение — `fgets`

Читает одну строку из файла в буфер.

```c
char line[256];
while (fgets(line, sizeof(line), file)) {
    printf("%s", line);
}
```

> ⚠️ `fgets()` сохраняет символ переноса строки `\n`, если он присутствует в файле.

## Перемещение по файлу

Иногда необходимо перейти к определённой позиции в файле.
Для этого используются функции **`fseek`** и **`ftell`**.

### `fseek`

```c
int fseek(FILE *stream, long offset, int origin);
```

| Параметр | Значение                        |
| -------- | ------------------------------- |
| `offset` | смещение в байтах               |
| `origin` | точка отсчёта:                  |
| —        | `SEEK_SET` — от начала файла    |
| —        | `SEEK_CUR` — от текущей позиции |
| —        | `SEEK_END` — от конца файла     |

#### Пример:

```c
fseek(file, 0, SEEK_END);
long size = ftell(file); // получить размер файла
printf("Размер файла: %ld байт\n", size);
fseek(file, 0, SEEK_SET); // вернуться в начало
```

## Проверка состояния файла

| Функция                  | Описание                                                  |
| ------------------------ | --------------------------------------------------------- |
| `feof(FILE *stream)`     | Возвращает ненулевое значение, если достигнут конец файла |
| `ferror(FILE *stream)`   | Проверяет наличие ошибки при работе с файлом              |
| `clearerr(FILE *stream)` | Сбрасывает флаги ошибок                                   |

#### Пример:

```c
if (feof(file)) {
    printf("Достигнут конец файла\n");
}
if (ferror(file)) {
    printf("Ошибка при чтении файла!\n");
}
```

## Пример программы

Программа, читающая текстовый файл построчно и выводящая его содержимое на экран:

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("example.txt", "r");
    if (file == NULL) {
        printf("Не удалось открыть файл.\n");
        return 1;
    }

    char line[256];
    while (fgets(line, sizeof(line), file)) {
        printf("%s", line);
    }

    fclose(file);
    return 0;
}
```

> 💡 `fgets` безопаснее, чем `fscanf`, так как не приводит к переполнению буфера.

---

## Заключение

Работа с файлами в C основана на концепции **потоков ввода-вывода**, представленных через структуру `FILE`.
Основные этапы:

1. Открыть файл (`fopen`)
2. Проверить успешность открытия
3. Выполнить операции чтения или записи (`fread`, `fwrite`, `fprintf`, `fscanf`, `fgets`)
4. При необходимости переместиться по файлу (`fseek`)
5. Закрыть файл (`fclose`)

> ✅ Всегда закрывайте файлы и проверяйте ошибки, чтобы избежать потери данных.

## Рекомендуемая литература

* Metanit.com: [Работа с файлами в языке C](https://metanit.com/c/tutorial/7.1.php)
* cppreference: [stdio.h functions](https://en.cppreference.com/w/c/io)
